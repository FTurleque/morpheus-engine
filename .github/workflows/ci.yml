name: MORPHEUS CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: morpheus-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: exact-head (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          check-latest: false

      - name: Enable executable scripts on Linux
        if: runner.os == 'Linux'
        shell: bash
        run: chmod +x mvnw scripts/*.sh distribution/*.sh

      - name: Run one-command M21 gate on Linux
        if: runner.os == 'Linux'
        shell: bash
        run: ./scripts/validate-m21.sh 1.0.0

      - name: Run one-command M21 gate on Windows
        if: runner.os == 'Windows'
        shell: cmd
        run: validate-m21.cmd -Version 1.0.0

      - name: Upload M21 integrity evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m21-integrity-${{ runner.os }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            **/target/surefire-reports/*.xml
            **/target/site/jacoco/jacoco.xml
            morpheus-architecture-tests/target/m21-coverage-summary.txt
            target/m21-supply-chain/**
            validation-output/m21/validation-summary.txt
            validation-output/m21/api.stdout.log
            validation-output/m21/api.stderr.log
