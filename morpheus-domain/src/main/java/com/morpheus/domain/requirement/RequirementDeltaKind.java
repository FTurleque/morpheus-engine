package com.morpheus.domain.requirement;

/** Structural effect declared by a change on a requirement, without M3 temporal projection. */
public enum RequirementDeltaKind {
    ADDED,
    MODIFIED,
    REMOVED
}
