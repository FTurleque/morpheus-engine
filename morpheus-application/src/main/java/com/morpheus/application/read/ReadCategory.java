package com.morpheus.application.read;

/** Provider-neutral categories that can be requested from a specification content reader. */
public enum ReadCategory {
    CURRENT_SPECIFICATIONS,
    REQUIREMENTS,
    SCENARIOS,
    CHANGES,
    REQUIREMENT_DELTAS,
    CONSTRAINTS,
    DESIGN_DECISIONS,
    IMPLEMENTATION_TASKS,
    ACCEPTANCE_CRITERIA,
    EXTERNAL_REFERENCES,
    ARCHIVES
}
