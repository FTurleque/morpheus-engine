package com.morpheus.application.analysis;

/** Documentary dimensions that can differ between the CURRENT baseline and one proposed requirement delta. */
public enum RequirementChangeField {
    PRESENCE,
    SPECIFICATION,
    KEY,
    TITLE,
    STATEMENT,
    SCENARIOS
}
