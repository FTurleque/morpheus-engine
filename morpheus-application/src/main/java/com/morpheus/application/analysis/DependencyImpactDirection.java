package com.morpheus.application.analysis;

/** Direction of an explicit DEPENDS_ON path relative to one directly changed requirement. */
public enum DependencyImpactDirection {
    DEPENDENCY,
    DEPENDENT
}
