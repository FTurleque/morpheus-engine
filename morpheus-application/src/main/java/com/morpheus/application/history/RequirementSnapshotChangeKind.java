package com.morpheus.application.history;

/** Minimal published-snapshot comparison taxonomy for M3-S6. */
public enum RequirementSnapshotChangeKind {
    ADDED,
    MODIFIED,
    REMOVED,
    UNCHANGED
}