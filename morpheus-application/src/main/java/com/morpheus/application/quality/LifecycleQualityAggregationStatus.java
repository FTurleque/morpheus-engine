package com.morpheus.application.quality;

/** Why lifecycle transition quality is not folded into a snapshot-only aggregate report. */
public enum LifecycleQualityAggregationStatus {
    REQUIRES_EXPLICIT_LIFECYCLE_INPUT
}
